const path=require("path"),{REST,Routes}=require("discord.js"),{createClient}=require("./client"),{loadCommands}=require("./commandLoader"),{loadEvents}=require("./eventLoader"),{initializeDatabase}=require("../database/firestore"),config=require("../config/config"),logger=require("../utils/logger");async function registerCommands(commands){const rest=new REST({version:"10"}).setToken(config.discord.token),body=[...commands.values()].map(c=>c.data.toJSON());await rest.put(Routes.applicationGuildCommands(config.discord.clientId,config.discord.guildId),{body});logger.info("Registered "+body.length+" guild command(s).");}async function start(){initializeDatabase();const client=createClient(),commands=loadCommands(path.join(__dirname,"..","commands"));client.commands=commands;loadEvents(client,path.join(__dirname,"..","events"));await registerCommands(commands);await client.login(config.discord.token);}module.exports={start};