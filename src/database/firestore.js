const {cert,getApps,initializeApp}=require("firebase-admin/app"),{getFirestore}=require("firebase-admin/firestore"),config=require("../config/config"),logger=require("../utils/logger");let db=null;function initializeDatabase(){if(db)return db;const app=getApps().length?getApps()[0]:initializeApp({credential:cert({projectId:config.firebase.projectId,clientEmail:config.firebase.clientEmail,privateKey:config.firebase.privateKey.replace(/\\n/g,"\n").replace(/\\r/g,"\r")})});db=getFirestore(app);logger.info("Firestore initialized.");return db;}function getDatabase(){return db||initializeDatabase();}module.exports={initializeDatabase,getDatabase};